# MCP Server Configuration

Dieses Projekt benötigt **n8n-mcp-tools**.

## Installation
```bash
mkdir -p ~/mcp-servers
cd ~/mcp-servers
git clone https://github.com/DEIN-USERNAME/n8n-mcp-tools.git
cd n8n-mcp-tools
npm install
```

## Claude Code Config

`~/.config/claude-code/config.json`:
```json
{
  "mcpServers": {
    "n8n-tools": {
      "command": "node",
      "args": ["/home/USER/mcp-servers/n8n-mcp-tools/server.js"]
    }
  }
}
```
